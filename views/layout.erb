<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>
      Nexmo Mobile App Promotion
    </title>
    <meta name="viewport"
          content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">

    <link rel="stylesheet"
          href="style.css">

  </head>
  <body>
    <div id="container">
      <% if !flash.keys.empty? %>
        <% flash.keys.each do |key| %>
          <div id='flash_<%= key %>'>
            <%= flash[key] %>
          </div>
        <% end %>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
